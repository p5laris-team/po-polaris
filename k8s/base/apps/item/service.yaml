apiVersion: v1
kind: Service
metadata:
  name: item-service
  labels:
    app: p5laris-item
spec:
  type: ClusterIP
  selector:
    app: p5laris-item
  ports:
    - name: grpc
      port: 9093
      targetPort: 9093
    - name: http
      port: 8080
      targetPort: 8080
