apiVersion: v1
kind: Service
metadata:
  name: notification-service
  labels:
    app: p5laris-notification
spec:
  type: ClusterIP
  selector:
    app: p5laris-notification
  ports:
    - name: grpc
      port: 9098
      targetPort: 9098
    - name: http
      port: 8080
      targetPort: 8080
