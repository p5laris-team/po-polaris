apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  labels:
    app: p5laris-gateway
spec:
  type: ClusterIP
  selector:
    app: p5laris-gateway
  ports:
    - name: http
      port: 8080
      targetPort: 8080
